{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "yarn install --frozen-lockfile --production=false && yarn run build"
  },
  "deploy": {
    "healthcheckPath": "/health/live",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
